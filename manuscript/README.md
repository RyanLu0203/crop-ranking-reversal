# Manuscript

Issue #9 provides the canonical modular LaTeX manuscript. Run `make manuscript`
to regenerate every reported numeric macro from verified outputs and validate
section order, citations, claim levels, figure/table paths, author-owned
placeholders and closure of all 44 teacher-Draft content rows.

The entry point is `main.tex`; prose is divided among `sections/`, cover and
declaration material is in `frontmatter/`, generated values are in `generated/`
and manuscript-specific evidence maps are in `registries/`. No historical
manuscript or teacher-Draft number is canonical. Issue #10 owns final isolated
compilation and the supervisor-review package.
