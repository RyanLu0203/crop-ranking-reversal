# Immutable teacher Draft

These files are the canonical baseline for research content and model architecture only:

- `Crop_ranking_reversal_total.tex`
- `Crop_ranking_reversal_total.pdf`

They must never be edited in place. Expected hashes are stored in `provenance/checksums/teacher_draft_SHA256SUMS.txt` and enforced by tests and `scripts/validate_repository.py`.

Draft citations, data, parameters, numbers, figures, tables, results, and conclusions are illustrative and inadmissible until independently verified and regenerated.
