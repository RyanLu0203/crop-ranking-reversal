\section{Ranking does not identify allocation}

A crop ranking and an acreage allocation are different mathematical objects. Let a suitability vector $s$ induce a weak order and let $x\in\mathbb{R}^n_+$ denote acreage. Strictly increasing transformations preserve the order in $s$ but can change any proportional score-to-acreage rule. The ranking also supplies neither an objective nor a feasible set. Consequently, even a strict order cannot identify a cardinal acreage vector.

\subsection{Stochastic profit, feasibility and tail risk}

For crop $i$ and scenario $\omega$, write per-acre stochastic margin and portfolio profit as
\begin{equation}
\widetilde\pi_i(\omega)=\widetilde P_i(\omega)\widetilde Y_i(\omega)-C_i,
\qquad
\widetilde\Pi(x,\omega)=\sum_{i=1}^n x_i\widetilde\pi_i(\omega).
\end{equation}
Price and yield may be dependent within and across crops. The mean-margin vector $\mu=\E[\widetilde\pi]$ supplies cardinal levels and gaps that an ordinal score does not. The joint law of $\widetilde\pi$, not its crop-wise marginals alone, determines portfolio loss $L(x,\omega)=-\widetilde\Pi(x,\omega)$ and its tail.

The operational set records crop bounds, total land, budget, rotations, contracts and other shared linear requirements. With lower and upper bounds $\ell,u$, per-acre cash requirement $c$, land $A$, budget $B$ and shared restrictions $Gx\leq g$, it is
\begin{equation}
X=\{x\in\R_+^n:\ell\leq x\leq u,\;\mathbf 1^\top x\leq A,\;c^\top x\leq B,\;Gx\leq g\}.
\end{equation}
Rotation and contract restrictions can enter $Gx\leq g$ or tighten crop-specific bounds. This polyhedral representation makes feasibility and active faces transparent while allowing the risk-constrained problem to remain a linear programme under finite scenarios.

For confidence level $\alpha$, the canonical programme maximizes expected margin subject to a loss-CVaR ceiling $\kappa$,
\begin{equation}
\max_{x\in X}\;\mu^\top x
\quad\text{subject to}\quad
\cvar_{\alpha}[L(x)]\leq\kappa.
\label{eq:canonical}
\end{equation}
For scenarios $k=1,\ldots,m$ with probabilities $w_k$, a free threshold $v$ and excess variables $q_k\geq0$ give the linear representation
\begin{equation}
v+\frac{1}{1-\alpha}\sum_{k=1}^m w_kq_k\leq\kappa,
\qquad q_k\geq -\pi_k^\top x-v.
\label{eq:cvarlp}
\end{equation}
Equations~\ref{eq:canonical}--\ref{eq:cvarlp} separate expected value, downside risk and operations. They also show why a score order cannot reveal which part of the decision system is active.

\begin{table}[t]
\centering
\caption{\textbf{Objects required for ranking-to-allocation inference.} Public observability does not by itself establish that observed acreage solves the canonical programme.}
\label{tab:objects}
\small
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.7cm}>{\raggedright\arraybackslash}p{3.1cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}p{2.9cm}}
\toprule
Object & Typical status & Role in the decision & Claim available without it \\
\midrule
Suitability score $s$ & Observed or predicted & Supplies an ordinal weak order & Rank agreement only \\
Margin vector $\mu$ & Calibrated or private & Gives cardinal payoff levels and gaps & No value or acreage comparison \\
Joint law of $\widetilde\pi$ & Estimated or designed & Determines portfolio and tail loss & No dependence or risk mechanism \\
Operational set $X$ & Partly private & Defines land, budget, rotation and contract choices & No feasibility attribution \\
Optimal face $S_\kappa$ & Model-derived & Contains every objective-equivalent optimum & No possible/universal classification \\
Selector $\sigma$ & Declared decision rule & Chooses one reported optimizer & No selected-reversal claim \\
\bottomrule
\end{tabularx}
\end{table}

The distinction becomes sharper when the optimum is set-valued. Let $r_\alpha(x)=\operatorname{CVaR}_\alpha[-\widetilde\pi^\top x]$ be loss-CVaR. The optimal face is
\begin{equation}
S_\kappa=\arg\max\{\mu^\top x:x\in X,\;r_\alpha(x)\leq\kappa\}.
\end{equation}
For a ranked pair $s_i>s_j$, reversal is \emph{possible} if $x_j>x_i$ at some $x\in S_\kappa$, \emph{universal} if it holds at every $x\in S_\kappa$, and \emph{selected} if it holds at the allocation returned by a declared deterministic rule. These definitions prevent one solver vertex from being mistaken for the entire optimal face.

The selected comparison uses one deterministic selector $\sigma(S_\kappa)$ held fixed across counterfactuals. This makes replay reproducible; it does not convert selection-dependent reversal into a property of every optimizer. Computational classification therefore solves two additional linear programmes on the objective-equivalent face: one minimizes and one maximizes $x_j-x_i$. Their signs supply universal and possible reversal independently of the reported vertex.

Four exact two-crop constructions share the same coordinate system (Fig.~\ref{fig:geometry}). Cardinal margins can point against the external crop ranking. A bound or contract can place the entire feasible set in the reversal half-space. A binding downside-risk constraint can move the optimum without implying monotone crop-wise comparative statics. A flat optimal face can cross the rank boundary, making reversal possible but not universal. Identical-looking allocations can thus encode different claims.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{../figures/goal17/main/Figure2.pdf}
\caption{\textbf{A four-mechanism atlas on shared coordinates.} Each panel uses the same Corn--Soybean simplex and rank boundary but distinguishes the feasible region, objective direction and optimal point or face. Cardinal margins rotate the objective; operational restrictions clip the domain; downside risk truncates a preferred corner; and a set-valued optimum crosses the boundary. These are exact analytic constructions, not empirical estimates.}
\label{fig:geometry}
\end{figure}

Operational feasibility supplies a transparent universal certificate. If $s_i>s_j$ and
\begin{equation}
\sup_{x\in X}x_i+\tau_x<\inf_{x\in X}x_j,
\end{equation}
then every feasible allocation reverses the pair; $u_i+\tau_x<\ell_j$ is a simple sufficient condition. This result needs no downside-risk channel. Conversely, if an unconstrained expected-margin optimum is risk-feasible, the risk constraint leaves the optimal value unchanged, but one slack selected solution does not establish equality of optimal faces when optima are multiple.

The certificate is positive rather than merely cautionary. Once a lower Soybean requirement or Corn upper bound places all feasible allocations beyond the rank boundary, universal reversal follows before the objective or tail-risk term is evaluated. The geometric construction connects directly to the assigned operational experiment below. By contrast, the risk-limited construction is an exact existence result: it shows that downside risk can expose a reversing face, not that increasing dependence or tightening a CVaR ceiling must move every crop monotonically.

The complete first-order identity blocks a second shortcut. At an optimum, non-negative multipliers and a subgradient $d\in\partial r_\alpha(x)$ satisfy
\begin{equation}
0=-\mu+\lambda d+\gamma\mathbf 1+\beta c+G^\top\eta-a+b.
\end{equation}
For crops $i$ and $j$,
\begin{equation}
\mu_i-\mu_j=\lambda(d_i-d_j)+\beta(c_i-c_j)+(G^\top\eta)_i-(G^\top\eta)_j-(a_i-a_j)+(b_i-b_j).
\label{eq:kktpair}
\end{equation}
The common land multiplier cancels, whereas budget, shared-constraint and bound terms remain. Equation~\ref{eq:kktpair} describes local marginal balance; it does not rank acreage levels.

The identity nevertheless identifies pressure channels inside a declared model. The first term is the local loss-CVaR subgradient difference, the second is crop-specific budget pressure, the third collects shared operational restrictions, and the last two are lower- and upper-bound pressures. A non-zero term explains how stationarity is balanced at the solution. It is neither a global decomposition of planted acres nor evidence that the corresponding private restriction exists in an observational state-year.

The nested model path makes these distinctions explicit (Fig.~\ref{fig:decomposition}). Successive stages add cardinal margins, operations, downside risk and dependence to a common benchmark. Allocation shares, expected margin and loss-CVaR are shown in their native units; an all-subset Shapley decomposition reports attribution intervals and closes with maximum efficiency residual $\ShapleyResidual$. Signed pressure terms from the operational intervention show how margin pressure is balanced locally by budget, shared or boundary terms, with maximum stationarity residual $\KKTResidual$. These are accounting decompositions over a controlled model, not causal effects in observed acreage.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{../figures/goal17/main/Figure3.pdf}
\caption{\textbf{Nested allocation and mechanism decomposition.} The staged path reports crop allocation, expected margin and loss-CVaR in native units, all-subset Shapley contributions and signed KKT pressures from the operational intervention. Intervals summarize repeated simulations; pressure terms are local accounting identities rather than causal acreage effects.}
\label{fig:decomposition}
\end{figure}
